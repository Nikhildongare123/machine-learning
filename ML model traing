import pandas as pd 
from sklearn .model_selection import train_test_split
fromm sklearn.neighbors import knighborsclassifier
from sklearn.metrics import classification_report,accurcy_score

df=pd.read_csv("  ")
# x and y
x = df[["  "]]
y = df["    ]
x_train,x_test,y_train,y_test = train_test_split(x,y,test_size =   ,random_state= )

x_train 

model = kneighborsclassifier()
model.fit(x_train,y_train)
#testing
y_pred = model.predict(x_test)

y_test

best_acc = 0
best_stete = none
for i in range(100:
x_train,x_test,y_train,y_test = train_test_split(x,y,test_size =   ,random_state= i )
model.fit(x_train,y_train)
y_pred = model.predict(x_test)
acc = accuracy_score(y_test,y_perd)
# store the best accuracy and random stste 
if acc > best_acc:
best_acc = acc 
best_stete = i
print("best accuracy ")
print(f" random stete {best_stete} gave the highest acc of {best_acc:4f})
